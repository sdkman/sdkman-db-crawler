services:
  - docker
env:
  - TERM=dump
script:
  - ./compose run --rm gradle clean check
  - ./compose run --rm gradle shadowJar
cache:
  directories:
  - "$HOME/.gradle"
branches:
  only:
    - master
